# Aggregate for local dev — `pip install -r requirements.txt` still installs
# everything, as documented in CLAUDE.md/README.
# Images install a slim subset instead:
#   Dockerfile      → requirements/app.txt
#   Dockerfile.mcp  → requirements/mcp.txt
-r requirements/dev.txt
