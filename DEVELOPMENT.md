# Development Journal & Decision Log

It tracks *why* you made certain decisions—this is what professors love to see in the final report.

## Project Setup (Feb 1, 2026)

- **Tool Stack:** Finalised the tools that I will be using for this project.
- **Repository Initialized:** Set up core folder structure.
- **Orchestration Choice:** Decided on **LangGraph** over CrewAI/LangChain to allow for cyclic error handling and better state management during the "Staff Review" phase.
- **Observability:** Integrated **LangSmith** to ensure every LLM call is traceable for the "Audit & Metrics" layer.
- **Environment:** Configured `.env` for Gemini (temporary).

## Phase 1 Goals: The Triage Node

- [X] Define Pydantic schema for structured output.
- [X] Craft the "Triage System Prompt" with clinical urgency rules.
- [X] Build a local test script to verify categorization accuracy.
- [X] Get started with setting up LangGraph (Basic workflow).
- [X] Integrate LangSmith, to improve observability which will help me with debugging.

## Phase 2 Goals: Login, Patient Identity & UI

- [X] **Login/register flow** — So every message is tied to a known patient (patient ID, full name, email). Enables staff to identify whose message is whose and supports future personalization.
- [X] **Auth layer** — `auth.py` supports Supabase (Auth + `profiles` table) when `SUPABASE_URL` and `SUPABASE_ANON_KEY` are set; otherwise **demo mode** (in-memory users) so the app runs without any backend. Decision: avoid blocking development on Supabase setup.
- [X] **Session state** — `state.py` holds current patient context (user_id, patient_id, full_name, email) after login; cleared on logout.
- [X] **Messages store** — `messages_store.py` saves each message with patient context and optional triage result; uses Supabase `messages` table when configured, else in-memory list for demo.
- [X] **Patient vs Staff tabs** — Two tabs after login: **Patient view** (send message, own history) and **Staff view** (all messages grouped by patient). Decision: makes it easy to simulate both patient and staff in one session for demos.
- [X] **Supabase schema** — `supabase_schema.sql` defines `profiles` (id, full_name, patient_id) and `messages` (user_id, patient_id, full_name, email, content, triage_result) with RLS. Staff can see all messages only with a separate policy or service role (documented in the SQL file).
- [X] **Repo hygiene** — `.gitignore` updated: `.cursor/` and `**/.cursor/` so Cursor IDE worktrees/cache are never committed; `__pycache__/`, `*.pyc`, `*.pyo` so Python cache is not committed. Ensures only the repo root (e.g. `Desktop/capstone/TriageAI`) is the source of truth.

## Phase 3 Goals: Agentic Refactor — Sprint 1 (MCP Tool Layer)

- [X] **Folder restructure** — Reorganized the entire project from a flat collection of root-level scripts into a modular, package-based architecture. The old layout had `safety_agent.py`, `triage_test.py`, `policy_agent.py`, `workflow.py`, `state.py`, `auth.py`, `messages_store.py`, and `streamlit_app.py` all at the project root. This made imports fragile and coupling tight. Moved everything into purpose-specific packages: `agents/`, `graph/`, `app/`, `mcp/`, `schemas/`. Decision: this structure is required before introducing MCP and multi-agent orchestration — agents need to call tools through a clean interface, not reach into sibling scripts.
- [X] **Schema migration** — `SafetyResult` and `TriageResult` Pydantic models now live in `schemas/schemas.py` with clean re-exports from `schemas/__init__.py`. The old root-level `schemas.py` shim was removed. Decision: a single source of truth for data models prevents circular imports as more agents are added.
- [X] **Graph layer split** — The monolithic `workflow.py` was split into three files: `graph/state.py` (TypedDict state definitions + PatientContext dataclass), `graph/nodes.py` (safety_node, triage_node functions), and `graph/workflow.py` (graph construction + `run_triage_workflow` entry point). Decision: separating state, nodes, and routing prepares for Sprint 2 where we add Context_Loader_Node and Tool_Node — adding a node should mean editing `nodes.py` and `workflow.py`, not untangling a single file.
- [X] **MCP tool layer** — Implemented three standalone tool functions for the MCP protocol:
  - `get_patient_history(patient_id)` in `mcp/tools/database_tools.py` — queries Supabase `profiles` table, returns `medical_history` string.
  - `search_hospital_policy(query, top_k)` in `mcp/tools/rag_tools.py` — wraps ChromaDB RAG search from `agents/policy_agent.py`, returns top-k policy chunks.
  - `get_available_slots()` in `mcp/tools/database_tools.py` — hardcoded list for Milestone 1 (`["Mon 10am", "Wed 2pm", "Fri 9am"]`), placed in database_tools to allow future Supabase table migration.
  - Decision: each tool is a pure function with no Streamlit dependency so the LangGraph agent can call them directly via MCP in Sprint 2.
- [X] **Communication tool** — Created `mcp/tools/communication.py` with `send_resolution_email` (extracted from the Streamlit app) and `send_notification`. Currently mock implementations. Decision: isolating communication as an MCP tool means the agent can eventually trigger emails/notifications as tool calls rather than relying on UI button handlers.
- [X] **MCP server** — `mcp/server.py` exposes all tools with clean imports and `__all__`. This is the single interface the agent will use to discover and call tools.
- [X] **Triage agent rename** — `triage_test.py` renamed to `agents/triage_agent.py`. Decision: the old name implied it was a test script, not a production agent. The function name `test_triage` was kept for now to avoid breaking the workflow, but the module name reflects its actual role.
- [X] **Import updates** — All imports across the codebase updated to use package-qualified paths (e.g., `from agents.safety_agent import screen_for_emergency` instead of `from safety_agent import ...`). `app/streamlit_app.py` adds the project root to `sys.path` so all package imports resolve correctly when run via `streamlit run app/streamlit_app.py`.
- [X] **Verification tests** — Created `tests/test_tools.py` with 8 tests covering all 3 MCP tools, schema imports, MCP server exports, ChromaDB relevance, and top_k behavior. All pass. Decision: the PRD requires a test script that "successfully queries ChromaDB and Supabase through the new tool functions" — this satisfies that and provides a regression baseline for Sprint 2.
- [X] **run_app.sh updated** — Entry point changed from `streamlit run streamlit_app.py` to `streamlit run app/streamlit_app.py`.

## Phase 4 Goals: Agentic Orchestrator & Reasoning Loop — Sprint 2

- [X] **Cyclic agentic graph** — Replaced the linear Safety → Triage pipeline with a cyclic LangGraph workflow. The new graph: `safety_node → (emergency? END : triage_agent_node) → (tool_calls? tool_node → triage_agent_node : synthesis_node) → END`. The triage agent can now loop: call tools, read results, call more tools, and only exit when it has enough context to produce a final assessment. Decision: this is the core architectural shift from "chatbot" to "Clinical Decision Support System" — the AI dynamically pulls from Supabase (history) and ChromaDB (policy) to ground its decisions.
- [X] **State redesign (`graph/state.py`)** — Added `messages` field with LangGraph's `add_messages` reducer so tool outputs append to a running conversation log instead of overwriting the original message. Added `patient_id`, `medical_history`, `policy_context`, `draft_reply`, `is_emergency` (short-circuit flag), and `staff_approved` (for future HITL). Decision: the `messages` list is essential for agentic loops — the LLM needs to "see" its own tool calls and the results to reason about what to do next.
- [X] **LangChain tool wrappers** — Wrapped the 3 MCP tools (`get_patient_history`, `search_hospital_policy`, `get_available_slots`) as LangChain `@tool` functions in `graph/nodes.py`. These are bound to the Gemini model via `.bind_tools()` so the LLM can invoke them during reasoning. Decision: LangChain's tool interface is required for `langgraph.prebuilt.ToolNode` to automatically route tool calls to the correct Python functions.
- [X] **Triage agent node** — Implemented `triage_agent_node` using `ChatGoogleGenerativeAI` (Gemini 2.5 Flash) with all 3 tools bound. The system prompt instructs the agent to: (1) analyze the patient message, (2) call tools as needed for history, policy, or scheduling, (3) produce a final triage assessment. The node returns an AIMessage that either contains tool_calls (routed to tool_node) or a final response (routed to synthesis). Decision: binding tools to the LLM and letting it decide what to call (rather than hardcoding the sequence) enables multi-intent handling — e.g., a single message asking for both a refill and an appointment triggers both `search_hospital_policy` and `get_available_slots`.
- [X] **Tool node** — Used `langgraph.prebuilt.ToolNode` which automatically maps LLM `tool_calls` to the corresponding Python functions and returns ToolMessages. Connected back to `triage_agent_node` to form the reasoning loop. Decision: ToolNode is the standard LangGraph pattern — it handles argument parsing, error wrapping, and message formatting, so we don't need to implement any of that manually.
- [X] **Synthesis node** — Added a `synthesis_node` as the final step before END. It extracts the TriageResult from the agent's conversation using a two-phase strategy: (1) try to parse JSON from the agent's last message, (2) if that fails, make an explicit structured extraction call to Gemini with `response_schema=TriageResult`. Decision: separating synthesis from the reasoning loop is more robust — the tool-calling Gemini model produces natural language, and the synthesis step reliably converts it to the structured schema the staff dashboard expects.
- [X] **Safety short-circuit** — When the safety node detects an emergency (`is_emergency=True`), the graph routes directly to `synthesis_node`, skipping the triage agent entirely. The synthesis node then tags the result with `safety_flagged`, `safety_reason`, and overrides urgency to `EMERGENCY`. Decision: avoids unnecessary LLM latency for emergencies — the safety gate catches it quickly and immediately exits. *(Note: Sprint 6 replaced the rule-based screen with an LLM call and fixed synthesis_node to only override urgency for confirmed emergencies.)*
- [X] **Routing logic** — Implemented `_route_after_safety` (emergency → synthesis, else → triage_agent) and `_should_continue` (tool_calls → tool_node, else → synthesis) as conditional edge functions. Decision: these two functions define the entire control flow of the agentic loop.
- [X] **Streamlit integration** — Updated `_run_workflow(msg, patient_id)` in `app/streamlit_app.py` to pass the patient's `patient_id` from session state to the workflow. The `run_triage_workflow` function signature now accepts `patient_id` but remains backward-compatible (defaults to empty string). Decision: the agent needs the patient_id to call `get_patient_history`.
- [X] **New dependency** — Added `langchain-google-genai>=2.0.0` to `requirements.txt`. This provides `ChatGoogleGenerativeAI` which supports `.bind_tools()` for Gemini function calling within LangGraph. Decision: needed for the standard LangGraph + Gemini agentic pattern (ToolNode expects LangChain message types).
- [X] **End-to-end tests** — Verified 3 scenarios:
  - Emergency ("sharp chest pain"): Safety gate catches it via rule match → short-circuits → EMERGENCY urgency. No LLM triage call made.
  - Multi-intent ("refill Lisinopril + schedule appointment"): Agent calls `search_hospital_policy` and `get_available_slots` → loops back with results → synthesizes structured TriageResult with intent "Refill", NORMAL urgency, and relevant checklist.
  - Admin ("billing question"): Agent calls `search_hospital_policy` → synthesizes with intent "Billing", queue "Billing".

## Phase 5 Goals: Human-in-the-Loop & System Calibration — Sprint 3 + Sprint 6 patch

- [X] **LLM-based safety screening (`agents/safety_agent.py` + `graph/nodes.py`) — Sprint 6 (Apr 2026)** — Replaced the entire regex-based safety layer with a single context-aware LLM call. The previous two-stage approach (regex patterns → LLM confirmation) still produced false positives because regex patterns like `\bheart\s+attack\b` matched historical mentions (e.g. "I had a heart attack five years ago and just need a refill"). Even when the LLM Stage 2 correctly identified these as non-acute (`triggered_by="rules_only"`), a bug in `synthesis_node` blindly overrode urgency to EMERGENCY for any `is_potential_emergency=True` — ignoring the triage agent's context-informed assessment. New logic: a single LLM screening prompt evaluates whether the message describes an ACTIVE, CURRENT, LIFE-THREATENING emergency. The prompt explicitly handles: past medical history ("I had", "history of", "five years ago"), chronic/stable conditions, medication refills that mention serious past conditions, and managed conditions (epilepsy, asthma, chronic pain). `triggered_by` is now either `"llm"` (confirmed emergency, short-circuits graph) or `"none"` (normal triage flow). Also fixed `synthesis_node` to check `state.get("is_emergency")` instead of `safety.get("is_potential_emergency")` — only overrides urgency for confirmed emergencies that short-circuited the graph, not for cases the triage agent already assessed. LLM failure still defaults to `False` (avoids false positives from outages). Decision: regex patterns are inherently context-blind — they match keywords regardless of tense, negation, or surrounding context. An LLM call costs ~200ms more but eliminates an entire class of false positives that no amount of regex refinement could fix. The synthesis_node fix ensures the triage agent's assessment is respected when the safety screen routes through it.
- [X] **State extension (`graph/state.py`)** — Added `patient_email: Optional[str]` (needed for the communication node to send emails) and `hitl_status: Optional[str]` (tracks workflow state: `"pending_review"`, `"approved"`, `"auto_completed"`, `"dismissed"`). Decision: these fields are essential for the HITL flow — the Streamlit app uses `hitl_status` to determine whether to show a message in the Pending Approvals tab.
- [X] **Draft reply node (`graph/nodes.py`)** — New `draft_reply_node` uses the policy agent's RAG (`get_relevant_policy` + `generate_draft_reply`) to produce a policy-grounded draft reply for every triaged message. Runs after synthesis, before any communication. Decision: separating draft generation from communication enables staff to review and edit the draft before it's sent.
- [X] **Communication node (`graph/nodes.py`)** — New `communication_node` calls `send_resolution_email` with the (potentially edited) draft reply. Sets `staff_approved=True` and `hitl_status="approved"`. This is the node that gets interrupted by the HITL checkpointer. Decision: making communication a discrete graph node (rather than inline Streamlit logic) means the graph can pause here and the UI is decoupled from the action.
- [X] **MemorySaver checkpointer (`graph/workflow.py`)** — Integrated `langgraph.checkpoint.memory.MemorySaver` as the persistence layer. Every node's output is checkpointed against a `thread_id`, enabling state recovery and mid-workflow pauses. Decision: `MemorySaver` is the right choice for this milestone — zero setup, no external database. In production, this would be swapped for `SqliteSaver` or a Redis-backed checkpointer.
- [X] **Conditional interrupt strategy** — Used `interrupt_before=["communication_node"]` with two separate end-path nodes: `communication_node` (interrupted, for NORMAL/HIGH/EMERGENCY) and `auto_communicate` (not interrupted, for LOW). The routing function `_route_after_draft` checks `triage_result.urgency` to decide the path. Decision: LangGraph's `interrupt_before` is statically defined at compile time — it always interrupts the named node. To allow LOW-urgency messages to flow through without staff review, we route them to a separate `auto_communicate` node that is not in the interrupt list. Both nodes call the same `send_resolution_email` function.
- [X] **Thread ID support** — `run_triage_workflow` now accepts an optional `thread_id` (auto-generates a UUID if not provided). The thread_id is embedded into `triage_result` so the Streamlit app can later resume the workflow. Decision: thread_id is the key for state recovery — each patient message gets a unique thread, and the staff dashboard uses it to call `get_workflow_state` and `resume_workflow`.
- [X] **Resume workflow** — New `resume_workflow(thread_id, edited_draft)` function in `graph/workflow.py`. If staff edited the draft, it calls `app.update_state(config, {"draft_reply": edited_draft})` before resuming with `app.invoke(None, config)`. Decision: this is the standard LangGraph HITL pattern — update state, then invoke with `None` to continue from the interrupt point.
- [X] **State inspection** — New `get_workflow_state(thread_id)` function returns the full state dict for a given thread. Used by the staff dashboard to display the interrupted workflow's triage result, draft reply, and safety flags. Decision: exposes the checkpointer's state without requiring the staff to understand LangGraph internals.
- [X] **Streamlit HITL dashboard** — Added a third tab **"Pending Approvals"** to the Streamlit app. It lists all messages where `triage_result.hitl_status == "pending_review"`, showing the AI analysis, an editable `st.text_area` for the draft reply, and "Approve & Send" / "Dismiss" buttons. Approve calls `resume_workflow` and updates the message store. Dismiss marks the message as dismissed. Decision: three tabs (Patient / Staff / Approvals) keeps the workflow clear — patients submit, staff reviews the queue, and the approvals tab is specifically for HITL intervention.
- [X] **Patient email pass-through** — Updated `_run_workflow` in `app/streamlit_app.py` to pass `patient_email=patient.email` through to the workflow. The communication node uses this to send emails. Decision: the patient's email was already in session state from login; now it flows through the entire graph.
- [X] **HITL status badges** — Staff queue now shows status badges: ⏸️ Pending, ✅ Sent, ⚡ Auto for each message based on `hitl_status`. Patient view hides internal fields (`thread_id`, `hitl_status`, `draft_reply`) from the triage summary.
- [X] **End-to-end tests** — Verified 4 scenarios:
  - Safety calibration: "chest pain when exercising" → NOT flagged by rules (PASS). "Sudden crushing chest pain" → flagged (PASS). "Heart racing alone" → NOT flagged (PASS). "Heart racing with dizziness" → flagged (PASS).
  - Emergency HITL: "Severe crushing chest pain" → safety flags → synthesis → draft_reply → **interrupted** before communication_node. `get_workflow_state` retrieves draft. `resume_workflow` with edited draft → email sent → `hitl_status = "approved"`.
  - Routing logic: LOW → `auto_communicate` (no interrupt). NORMAL/HIGH/EMERGENCY → `communication_node` (interrupted).
  - Sprint 1 regression: All 8 original MCP tool tests still pass.

## Phase 6 Goals: Refactor RAG to Official Chroma MCP Server — Sprint 4

- [X] **Persistent vector store (`agents/policy_agent.py`)** — Replaced `chromadb.EphemeralClient()` with `chromadb.PersistentClient(path="./data/vector_store")`. Collection renamed from `"policy"` → `"hospital_policies"`. Inline fallback seed retained for backward compatibility (if seed script hasn't been run). Decision: persistent storage means policy documents survive app restarts without re-seeding.
- [X] **Seed script (`scripts/seed_policy.py`)** — Idempotent one-time script that populates the persistent vector store with 7 default clinic policy documents. Uses `get_or_create_collection` + count check so running it twice is safe. Decision: separating seeding from runtime initialization makes deployment explicit and predictable.
- [X] **MCP config (`mcp_config.json`)** — Added stdio transport config for `chroma-mcp-server` with `--client-type persistent --data-dir ./data/vector_store`. This is read by `MultiServerMCPClient` to launch the Chroma MCP server as a subprocess. Decision: externalizing the config makes it easy to add more MCP servers later.
- [X] **Tool splitting (`graph/nodes.py`)** — Exported `LOCAL_TOOLS` (non-MCP: `get_patient_history`, `get_available_slots`) and kept `TRIAGE_TOOLS` as the fallback list including local `search_hospital_policy`. Parameterized `_build_triage_model(tools=None)` and created `_make_triage_agent_node(tools)` closure factory so the graph builder can inject MCP-discovered tools. Updated system prompt to use generic tool references. Decision: decoupling tool discovery from node construction enables MCP tools to be swapped in transparently.
- [X] **Async MCP graph builder (`graph/workflow.py`)** — Added `build_graph_async()` which discovers MCP tools via `MultiServerMCPClient`, merges them with `LOCAL_TOOLS`, and builds the graph with the merged tool list. `build_graph()` stays sync — wraps `asyncio.run(build_graph_async())` with try/except fallback to `_build_graph_local_only()`. `nest_asyncio.apply()` called at module level for Streamlit compatibility. Decision: the async bridge pattern keeps the entire public API synchronous while enabling MCP tool discovery.
- [X] **Streamlit update (`app/streamlit_app.py`)** — Added `nest_asyncio` import and `apply()` at top. No other changes. Decision: `nest_asyncio` is required because Streamlit already runs an event loop and `asyncio.run()` would fail without it.
- [X] **Deprecation note (`mcp/tools/rag_tools.py`)** — Added deprecation docstring explaining that `search_hospital_policy` is now a fallback; the MCP server's `chroma_query_documents` is preferred when available. No functional change. Decision: keeps the module for backward compatibility but signals the direction.
- [X] **New dependencies** — Added `chroma-mcp-server>=0.2.0`, `langchain-mcp-adapters>=0.1.0`, `nest-asyncio>=1.6.0` to `requirements.txt`.
- [X] **Tests (`tests/test_tools.py`)** — Added 5 Sprint 4 tests: persistent store collection check, persistent store query, MCP config validation, local-only graph fallback build, and tool list export verification. All Sprint 1 tests still pass.
- [X] **Fallback safety** — If `chroma-mcp-server` fails to start or `langchain-mcp-adapters` can't discover tools, `build_graph()` catches the exception and falls back to `_build_graph_local_only()` which uses `TRIAGE_TOOLS` (includes local `search_hospital_policy`). The app degrades gracefully to Sprint 3 behavior.

## Phase 7 Goals: Multimodal Streaming Assistant — Sprint 5

- [X] **Multimodal state fields (`graph/state.py`)** — Added `file_uri` (base64 data URI for attached files), `file_mime_type`, `file_name`, and `is_complete` (conversational interrupt control) to `TriageWorkflowState`. Decision: base64 data URIs are JSON-serializable and self-contained — no temp file cleanup needed. The `is_complete` flag prevents the checklist gate from re-interrupting after the patient has already answered.
- [X] **Visual safety screen (`graph/nodes.py`)** — Added `_visual_safety_screen()` that uses Gemini vision to check attached images for emergency red flags (active bleeding, respiratory distress, cyanosis, trauma, severe burns, anaphylaxis). Runs after text-based screening only when an image is attached and text screening didn't flag emergency. Decision: vision is additive for image-specific emergencies that text alone can't catch. *(Note: Sprint 6 replaced the rule-based text screen with an LLM call; the visual screen still runs as an additional layer for image attachments.)*
- [X] **Multimodal triage (`graph/nodes.py`)** — Extended `_triage_agent_node_impl` to build multimodal `HumanMessage` content (text + image_url) when `file_uri` is an image. For PDFs, notes the attachment in text. Falls back to plain text when no file. Decision: the Gemini 2.5 Flash model supports multimodal content natively via LangChain's image_url content type.
- [X] **Checklist gate node (`graph/nodes.py`)** — New `checklist_gate_node` placed between triage_agent and synthesis. Parses the agent's last message for checklist items. If items are present, calls `interrupt(question)` to pause the graph and ask the patient. On resume, `interrupt()` returns the patient's answer, which is appended as a HumanMessage and `is_complete` is set to True. Decision: keeps `triage_agent_node` unchanged; the gate is a separate concern that inspects output rather than modifying the agent's behavior.
- [X] **Triage system prompt hardened for conversational intake (`graph/nodes.py`) — Sprint 5 patch (Mar 2026)** — Rewrote `TRIAGE_SYSTEM_PROMPT` to explicitly instruct the agent to treat itself as a first-contact intake agent, not a one-shot classifier. Added a `## Checklist Rules` section that tells the model exactly when to ask follow-ups: symptoms always require duration/severity/progression; refills require medication name and dose; vague messages require a clarifying question before classification. Critically, added the instruction "Do NOT leave the checklist empty simply because a classification is possible" — without this, Gemini would produce a plausible triage result and an empty checklist even when key details were missing, bypassing the conversation loop entirely. Decision: the prompt is the contract between the LLM and the graph; the multi-turn loop only works if the model uses the checklist field actively. Explicit rules and a negative constraint are necessary to prevent the model from taking the path of least resistance (classify immediately and skip questions).
- [X] **Multi-turn conversational intake loop (`graph/nodes.py` + `graph/workflow.py`) — Sprint 5 patch (Mar 2026)** — Refactored the checklist gate from a single-interrupt pass-through into a true multi-turn conversation loop. Previously the gate asked one question, set `is_complete=True` on the patient's first answer, and immediately routed to synthesis — the triage agent never re-evaluated the answer. Now: (1) `checklist_gate_node` no longer sets `is_complete=True` after an interrupt; it only marks complete when the agent itself produces no remaining checklist items. (2) A new `_route_after_checklist` conditional edge routes back to `triage_agent_node` when more context is needed, or forward to `synthesis` when `is_complete=True`. The triage agent re-runs with the full accumulated `messages` history (every question + answer) and decides whether to ask again or finalize. This means staff only receive a case once the AI is fully satisfied — every clarification has already happened in-chat. Decision: the agent, not the gate, determines when the conversation is complete. This is architecturally correct because the agent knows what information it needs to produce a high-confidence triage result.
- [X] **Dual interrupt coexistence** — Patient interrupts (checklist_gate_node) happen during triage, before synthesis. Staff HITL interrupts (`interrupt_before=["communication_node"]`) happen later, before email delivery. They are sequential on the same thread, not conflicting. Decision: LangGraph supports multiple interrupts on the same thread; the graph simply pauses at whichever interrupt point is reached first, and `resume` continues to the next.
- [X] **Streaming entry points (`graph/workflow.py`)** — Added `stream_triage_workflow()` (returns app, initial_state, config, thread_id) and `resume_chat()` (returns app, Command(resume=answer), config). These are additive — `run_triage_workflow()` and `resume_workflow()` remain unchanged. Decision: returning the (app, inputs, config) tuple lets the caller drive `app.stream()` directly, enabling the Streamlit UI to iterate over tokens synchronously.
- [X] **Safe-stream bridge (`app/streaming.py`)** — New `stream_graph()` generator wrapping `app.stream(stream_mode='messages')`. Yields typed dicts: `token` (text chunk), `status` (tool call name), `interrupt` (follow-up question), `done`, `error`. After the stream exhausts, checks `app.get_state()` for pending interrupts. Decision: isolating the streaming logic in a bridge module keeps the Streamlit UI clean and makes the generator testable independently.
- [X] **Streaming chat UI (`app/streamlit_app.py`)** — Replaced the form-based patient portal with a `st.chat_message`/`st.chat_input` interface. Tokens stream progressively with a cursor animation. Sidebar file uploader (jpg, png, pdf) with image preview. Interrupt handling: when the AI asks a follow-up question, the patient replies via the same chat input and the graph resumes. Staff view and Pending Approvals tabs are completely unchanged. Decision: `st.chat_input` is Streamlit's standard conversational input; combining it with `st.chat_message` and progressive rendering provides a natural chat experience.
- [X] **Tests (`tests/test_tools.py`)** — Added 6 Sprint 5 tests: state multimodal fields, checklist gate passthrough, checklist gate empty checklist, streaming bridge import, graph compilation with checklist_gate, and streaming entry point imports. All 19 tests pass (13 existing + 6 new).

## Current Roadblocks / Notes

- Need to read more research on this topic. (I have compiled some information in a word doc; I will make it public and paste the link here.) [Research Notes](https://docs.google.com/document/d/1Gr3C9JskVDrQheYMiz4jvEEy_VRp9OTWS1hD-DlHIaM/edit?usp=sharing)

## Sprint 6 Progress: Stability & Infrastructure

- [X] **SqliteSaver persistent checkpointer (`graph/workflow.py`) — Sprint 6 (Mar 2026)** — Replaced `MemorySaver` (in-memory, lost on restart) with `SqliteSaver` backed by `./data/checkpoints.db`. Uses `sqlite3.connect(path, check_same_thread=False)` + `SqliteSaver(conn)` + `setup()`. Falls back to `MemorySaver` with a `warnings.warn()` if `langgraph-checkpoint-sqlite` is not installed. Added `langgraph-checkpoint-sqlite>=1.0.0` to `requirements.txt`. Decision: HITL `thread_id`s must survive app restarts — without this, "Approve & Send" in Pending Approvals silently fails after any reload because the in-memory state is gone.
- [X] **Fix silent exception swallowing in `build_graph()` (`graph/workflow.py`) — Sprint 6 (Mar 2026)** — Replaced bare `except: pass` with `warnings.warn()` that surfaces the exact exception type and message when MCP tool discovery fails, and a separate warning when `mcp_config.json` is not found. The app still falls back to local-only tools, but now the active code path is visible in logs. Previously, the MCP server could silently fail every time and the developer would never know.

- [X] **Real email via Resend (`mcp/tools/communication.py`) — Sprint 6 (Mar 2026)** — Replaced the mock `send_resolution_email` (`print()` to stdout) with Resend transactional email. When `RESEND_API_KEY` is set in `.env`, emails are delivered to the patient via the Resend API. When the key is missing, falls back to the original console mock so the app works in demo mode without email config. `send_notification` now routes through `send_resolution_email` for the email channel. Added `resend>=2.0.0` to `requirements.txt` and documented `RESEND_API_KEY` + `RESEND_FROM_EMAIL` in `.env.example`. Decision: Resend over smtplib/SendGrid because it has the simplest integration (one API call, no SMTP config), a free tier sufficient for capstone demos, and graceful degradation when unconfigured.

- [X] **`medical_history` column in Supabase profiles (`supabase_schema.sql`) — Sprint 6 (Mar 2026)** — Added `medical_history text default ''` to the `profiles` table definition, plus an idempotent `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` statement for existing databases. The `get_patient_history` tool in `mcp_tools/tools/database_tools.py` already queries this field — it was returning `""` because the column didn't exist. Column has been added to the live Supabase project via `ALTER TABLE` and seeded with demo patient medical history (hypertension, Lisinopril 10mg, Type 2 diabetes, Metformin 500mg, seasonal allergies). The triage agent now has real patient context to reason with during tool calls. Resend API key (`RESEND_API_KEY`) has also been configured in `.env` so the HITL "Approve & Send" flow delivers actual emails. Decision: adding the column to the schema file means fresh deployments get it automatically; the ALTER handles upgrades.
- [X] **Staff "Edit draft reply" (`app/streamlit_app.py`) — Sprint 6 (Mar 2026)** — Replaced the three stub action buttons (Edit draft reply → "coming soon", Request more info → "coming soon") with a working implementation. Staff detail view now shows: (1) an editable `st.text_area` pre-filled with the HITL draft reply or a policy-agent-generated draft, (2) "Approve & Send" button that calls `resume_workflow(thread_id, edited_draft)` when an HITL thread exists, or sends directly via `send_resolution_email` otherwise, (3) "Route to ER" button for emergency escalation with patient notification, (4) "Dismiss" button that marks the case as dismissed. The Pending Approvals tab is unchanged — it remains the dedicated HITL review workflow. Decision: both views call the same `resume_workflow` function for HITL cases, but the Staff view additionally supports direct send for non-HITL messages (e.g. already auto-completed LOW urgency cases that staff want to follow up on).

- [X] **MCP folder rename & tool discovery fix (`mcp/` → `mcp_tools/`) — Sprint 6 (Mar 2026)** — Renamed the `mcp/` directory to `mcp_tools/` because `mcp/` shadowed the `mcp` pip package (`from mcp import ClientSession` resolved to the local folder instead of the installed package, breaking `MultiServerMCPClient`). Updated all imports across 8 files: `mcp_tools/server.py`, `mcp_tools/tools/__init__.py`, `graph/nodes.py`, `graph/workflow.py`, `app/streamlit_app.py`, `tests/test_tools.py`, `scripts/verify_mcp_tools.py`. After the rename, MCP tool discovery via `chroma-mcp-server` (stdio transport) works end-to-end — the Chroma MCP server starts as a subprocess, tools are discovered via `MultiServerMCPClient`, merged with `LOCAL_TOOLS`, and bound to the Gemini model. The local-only fallback is retained if the MCP server is unavailable. Decision: renaming the folder is the minimal fix; production deployment would use SSE transport to a remote host. Updated README project structure to reflect the rename.

- [X] **Staff reply persistence (`app/streamlit_app.py`) — Sprint 6 (Apr 2026)** — Staff replies were "fire and forget" — sent via email (Resend) but never stored in the database. The patient's "Your message history" section only displayed original patient messages; there was no mechanism to show what staff had replied. Fix: when staff clicks "Approve & Send", "Route to ER", or approves via Pending Approvals, the reply text is saved as `staff_reply` inside the existing `triage_result` JSONB column (no schema migration needed). The patient message history now displays the staff reply under each resolved message. All four send paths were updated: Staff "Approve & Send" (HITL resume), Staff "Approve & Send" (direct send), Staff "Route to ER", and Pending Approvals "Approve & Send". Decision: storing the reply in `triage_result` JSONB avoids a database schema change while keeping the full conversation visible to patients within the portal.

- [X] **Enhanced streaming chat UX (`app/streaming.py` + `app/streamlit_app.py`) — Sprint 6 (Apr 2026)** — Rewrote the safe-stream bridge to provide real-time status updates as the workflow progresses. The bridge now tracks graph node transitions via `metadata["langgraph_node"]` and yields human-friendly status events at each stage (e.g. "Screening for emergencies", "Analyzing your message", "Fetching patient history", "Searching clinic policies", "Preparing triage assessment", "Drafting reply"). Tool calls and tool results are mapped through a `_TOOL_LABELS` dictionary that translates internal names like `get_patient_history` → "Fetching patient history". The Streamlit renderer now shows status updates with a styled dot indicator instead of tiny `st.caption()` text, and streams text tokens progressively with a bold cursor. Also distinguishes between `AIMessage`/`AIMessageChunk` (tokens + tool calls) and `ToolMessage` (tool results returned) for more granular status reporting. Decision: the key UX insight is that patients need to see the system is working during the 5-15s it takes for the full triage cycle — showing each stage live makes the wait feel intentional rather than broken.
- [X] **Evaluation harness (`tests/eval_dataset.json` + `scripts/run_eval.py`) — Sprint 6 (Mar 2026)** — Built a labeled dataset of 26 patient messages covering: 5 true emergencies (cardiac, overdose, anaphylaxis, suicidal ideation, unconscious), 5 false positive traps (chronic back pain, managed epilepsy, stable asthma, exercise-related chest discomfort, past fainting), 3 refills, 3 appointments, 2 billing, 4 clinical questions, 2 high-urgency, and 2 multi-intent messages. The eval script (`run_eval.py`) runs each message through `run_triage_workflow`, compares output against expected labels, and produces a scorecard: safety recall/precision/FP rate, intent accuracy, urgency accuracy (exact and ±1 level). Supports `--safety-only` mode (fast, no triage LLM calls) and `--ids` filtering for targeted runs. Results are saved to `tests/eval_results.json` for inclusion in the capstone report. Decision: 26 messages is sufficient to demonstrate the system's accuracy characteristics and to show the impact of the two-stage safety confirmation (before/after FP rate comparison). Set `LANGSMITH_TRACING=true` in `.env` to trace all eval runs in the LangSmith dashboard.
## Sprint 7 Progress: Offline Evaluation Suite (LangSmith)

- [X] **MCP tool set completion & client lifetime fix (`mcp_tools/mcp_server.py`, `graph/nodes.py`, `graph/workflow.py`) — Sprint 7 (Jun 2026)** — Three polish fixes to the MCP path discovered while auditing how `get_patient_history` is served: (1) `mcp_tools/mcp_server.py` only exposed `get_patient_history`; added `get_available_slots` and `search_hospital_policy` so the `triageai-tools` MCP server exposes the full tool set. (2) `search_hospital_policy` was missing from `LOCAL_TOOLS`, so in the MCP path the policy-search tool was silently dropped (chroma-mcp-server only provides `chroma_query_documents`); added it. (3) `MultiServerMCPClient` was a local variable in `_init_mcp_tools` — once it was GC'd, the spawned stdio subprocesses could be killed before any tool call; it is now held in a module-level `_mcp_client` singleton for the process lifetime. Added `print()` path-active logs to both `build_graph_async` and `_build_graph_local_only` so the active tool set is visible at startup. Verified end-to-end: the MCP path now lists all three TriageAI tools alongside the chroma tools. Decision: keeping the local wrappers de-duped by name (MCP wins) means the same code works whether or not the MCP servers start.

- [X] **Unified eval dataset (`scripts/migrate_eval_datasets.py` → `tests/eval_dataset_unified.json`) — Sprint 7 (Jun 2026)** — Six dataset files had accumulated across Sprints 4–6 with two incompatible schemas (`message` vs `patient_message`, with/without `patient_history`/`note`). Wrote a migration that normalizes all of them onto one canonical schema (adds optional `clinical_reasoning`, `draft_must_mention`, `draft_must_not_mention`, `source` fields), dedupes by normalized message text (215 unique from 249 raw; 69 emergencies / 146 non-emergencies), and guarantees unique IDs. Old per-dataset files are left untouched as backups. Decision: dedup by message text (not ID) because IDs collided across files; one canonical source means every eval reads the same data and results are comparable.

- [X] **LangSmith dataset sync (`scripts/sync_langsmith_dataset.py`) — Sprint 7 (Jun 2026)** — Idempotent upload of the unified dataset to a LangSmith Dataset (`TriageAI-offline`). Each example maps to `inputs={message, patient_history}`, `outputs={expected_intent, expected_urgency, is_emergency}` (reference labels), and `metadata={id, category, clinical_reasoning, draft hints, source}`. Re-running reconciles by our stable record `id` stored in `metadata` — existing examples are updated in place, new ones created, nothing deleted. Verified: 215 created on first run, 215 updated (0 created) on re-run. Decision: LangSmith over the homegrown JSON scorecard because it gives run-over-run diffing in the UI for the capstone demo, and the `@traceable` hooks already existed.

- [X] **Evaluators: code + LLM-as-judge (`scripts/eval_evaluators.py`) — Sprint 7 (Jun 2026)** — Two evaluator families using the LangSmith `(run, example)` signature. Code-based (deterministic, free): `safety_correct`, `safety_recall` (emergency-only, returns None to skip non-emergencies so they don't dilute the aggregate), `urgency_exact`, `urgency_within_one`, `intent_match` (substring, mirrors `run_eval.py`). LLM-as-judge (Gemini structured output via the same `google.genai` path the app uses, with a `_JudgeVerdict` Pydantic schema): `draft_policy_grounded` (no fabricated policy), `draft_faithful` (on-topic, no hallucinated facts, honors `draft_must_not_mention`), `draft_tone` (empathetic, escalates emergencies, includes `draft_must_mention`). Judges return `None` score on LLM outage so an API hiccup doesn't manufacture a failing eval. Decision: draft-reply quality was the single biggest unmeasured gap — it is the actual product output — and an LLM judge is the only practical way to score grounding/faithfulness/tone at scale.

- [X] **Runner + regression gate (`scripts/run_langsmith_eval.py`) — Sprint 7 (Jun 2026)** — `target_fn` runs `run_triage_workflow` for each example and shapes `{safety, triage, draft_reply}` for the evaluators, pulling the draft from the persisted checkpoint state for LOW-urgency (auto_completed) cases where it isn't surfaced on `triage_result`. Calls `langsmith.evaluate(...)`, aggregates scores (ignoring `None`/skipped), prints a scorecard, and enforces thresholds — exiting non-zero on breach so it can gate CI. `safety_recall` is a HARD gate at 1.00 (missing an emergency always fails). Flags: `--limit`, `--code-only` (skip the LLM judges for a fast/cheap run), `--concurrency`, `--dataset-name`. Verified end-to-end with a 2-example `--code-only` run. Decision: the hard recall gate encodes the project's core safety invariant directly in the eval — a regression that misses an emergency can never pass.
